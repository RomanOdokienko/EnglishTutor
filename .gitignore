# Python bytecode cache
__pycache__/
*.pyc

# Secrets — never commit. The token doc holds the live OpenAI/AssemblyAI keys.
*.docx
.env
.env.*

# Skill tooling (installed via `npx skills add`) — not part of the app.
.agents/
.claude/skills/
skills-lock.json
